<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ygo.db.mapper.MonsterMapper">

	<select id="findMonster" resultType="com.ygo.db.model.Monster">
	   SELECT c_name,j_name,e_name,nw_name,psd,lev,attribute,race,atk,def,pend,link,img,ban,desc_nw,proprietary,hash_code FROM t_monster WHERE 1=1
	   <if test="ids != null">
	       AND hash_code IN
           <foreach collection="array" item="id" index="ids" open="(" close=")" separator=",">
	            #{id}
           </foreach>
	   </if>
	   <if test="mon.cName != null &amp;&amp; mon.cName != ''">
	       AND c_name LIKE CONCAT('%',#{mon.cName},'%')
	   </if>
	   <if test="mon.lev != null">
	       AND lev=#{mon.lev}
	   </if>
	   <if test="mon.attribute != null">
	       AND attribute=#{mon.attribute}
	   </if>
	   <if test="mon.race != null">
	       AND race=#{mon.race}
	   </if>
	   <if test="mon.atk != null">
	       AND atk=#{mon.atk}
	   </if>
	   <if test="mon.def != null">
	       AND def=#{mon.def}
	   </if>
	   <if test="mon.pend != null">
	       AND pend=#{mon.pend}
	   </if>
	   <if test="mon.link != null">
	       AND link=#{mon.link}
	   </if>
	   <if test="mon.ban != null">
	       AND ban=#{mon.ban}
	   </if>
	   LIMIT #{start},#{row}
	</select>

</mapper>